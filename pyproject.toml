[build-system]
requires = ["setuptools", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "practice_algo"
version = "0.1.0"

[tool.setuptools.packages.find]
where = ["."]
include = ["data*", "ml*", "leetcode*", "linear_alg*"]